#include<iostream>
#include<vector>

using namespace std;

int binary_sort(vector<int> matrix, int target) {
    int ans = INT_MIN;
    int in_size = matrix.size();
}

bool searchMatrix(vector<vector<int>>& matrix, int target) {
    int size = matrix.size();
    int first = 0, last = size - 1, mid = 0;
    for (int i = 0; i < size; i++)
    {
        
    }
    
}

int main(void)
{
    
    return 0;
}